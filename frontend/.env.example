VITE_API_URL=http://localhost:8000

# Feature Flags
# When true, fetches platform credentials from backend /api/platform/config endpoint
# When false, uses mock data only (no platform integration)
VITE_USE_PLATFORM_VIEWS=false

# Platform Configuration - MOVED TO BACKEND
# For security, platform credentials are now stored in backend environment variables.
# See root .env.example for backend configuration requirements.
