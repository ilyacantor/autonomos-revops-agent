# Backend Environment Variables

# AOS Platform Configuration (required when using platform views)
# These credentials are exposed via the /api/platform/config endpoint
# They are kept secure on the backend and never bundled into frontend code
AOS_BASE_URL=https://autonomos-platform.replit.app
AOS_TENANT_ID=demo-tenant
AOS_AGENT_ID=revops-dev
AOS_JWT=

# Note: When VITE_USE_PLATFORM_VIEWS=true in frontend/.env,
# the frontend will fetch these credentials from /api/platform/config
